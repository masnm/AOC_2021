After getting the input from the AOC website
first count the number of integer in the inpout file.

You can simply copy the number and put them into a fie.
and get the numbers by using `wc` command

	$ cat <input filename> | wc -l

now add the count in the first line of the input.

simply comment any of the function `part_one()` or `part_two()`
and run with the modified input.
